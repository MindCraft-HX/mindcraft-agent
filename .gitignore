# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# Local agent workspace state, diagnostics, and scratch analysis.
.codex/
.claude/
.cursor/
.continue/
.DS_Store
dist
dist-ssr
dist_electron
coverage
*.local
dist-electron
source
.history
plugins
!/public/plugins/
dev-plugins/
.worktrees/



/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Runtime databases (created in userData at runtime, not project assets)
*.db
*.db-journal
*.db-wal
*.db-shm

# Keep the npm lockfile reproducible. Package-manager debug files are ignored above.

# 文档本地草稿 / 私密记录（不提交）
docs/local/
docs/private/
docs/tmp/

# 市场宣发材料（不提交到代码仓库）
docs/marketing/

# 本地个人覆盖（不提交）；AGENTS.md / CLAUDE.md 是项目规则，需提交
CLAUDE.local.md
AGENTS.local.md

# Local credentials and machine-specific environment overrides. The tracked
# config/.env.* files contain only non-secret build targets.
.env
.env.*.local
.envrc
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer

# Electron/runtime artifacts and test output.
*.asar
*.blockmap
*.dmg
*.exe
*.msi
*.zip
playwright-report/
test-results/

# Local runtime files that may be created when userData is redirected to the repo.
session-registry/
simple-chat/
diagnostics/
mindcraft.db
mindcraft.db-*

__pycache__/
_test/
