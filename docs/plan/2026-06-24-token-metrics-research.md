# Token Metrics Research Archive

> Status: archived.
> Current contract: `docs/token-metrics-contract.md`.

This file used to contain the early T144-T146 investigation plan. Its old
provider formulas and phase notes are no longer authoritative.

Do not use this archive to implement metric fixes. Use:

- `docs/token-metrics-contract.md` for current semantics and acceptance rules.
- `docs/token-metrics.md` for the short compatibility entry point.
- `tests/*metrics*` and `packages/agent/electron/tokenMetrics/*.test.cjs` for
  executable contracts.
